{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "whisper-sidecar",
  "description": "Permissions for whisper-cli sidecar",
  "windows": ["main"],
  "permissions": [
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "name": "binaries/whisper-cli",
          "sidecar": true
        }
      ]
    }
  ]
}
